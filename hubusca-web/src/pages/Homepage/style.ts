import styled from "styled-components";

export const StyledHomePage = styled.main`
  position: relative;
  height: 100vh;
  width: 100vw;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--gap-60);
`;
